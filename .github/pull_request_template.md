## Qué cambia

<!-- En una frase, qué ve distinto el jurado. -->

## Comprobado

Si tocaste el bot (`src/`, `workflows/`, `db/`):

- [ ] `pnpm test` pasa

Si tocaste la landing (`web/`):

- [ ] `pnpm verificar` dentro de `web/` pasa (build + tipos)
- [ ] `pnpm humo` dentro de `web/` pasa, con la página levantada
- [ ] Si tocaste `web/lib/datos.ts`: cada número nuevo sale del repo o de una fuente citada
- [ ] Español colombiano, sin voseo

## Notas

<!-- Lo que el que revise necesita saber y no se ve en el diff. Si no hay, bórralo. -->
